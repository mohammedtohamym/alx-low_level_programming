#include <stdio.h>

/**
 * main - Prints a string with printf function
 *
 * Return: 0 if program runs and executed successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
