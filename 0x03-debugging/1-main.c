#include <stdio.h>

/**
 * main - infinite loop
 *
 * Return: 0
 */
int main(void)
{
	int eeei;

	printf("Infinite loop incoming :(\n");
	eeei = 0;

	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */

	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
